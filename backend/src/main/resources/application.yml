spring:
  application:
    name: petlife-backend

  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;databaseName=petlifeDB;encrypt=true;trustServerCertificate=true
    username: test
    password: 123

  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  web:
    cors:
     allowed-origins: ("http://localhost:5173","http://localhost:5174")
     allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
     allowed-headers: "*"
     allow-credentials: true


  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: petlifetest123@gmail.com
    password: bfpt wkcr aaqe essi
    
    properties:
      mail:
       smtp:
        auth: true
        starttls:
          enable: true

server:
  port: 8082

# JWT
jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000 # 1小時 (毫秒)

google:
 client-id: ${GOOGLE_CLIENT_ID}
 client-secret: ${GOOGLE_CLIENT_SECRET}
 redirect-uri: http://localhost:5173/login
 
gemini:
   api-key: ${GEMINI_API_KEY}
 
linepay:
  channel-id: ${LINEPAY_CHANNEL_ID} # 你的 LINE Pay Channel ID
  channel-secret: ${LINEPAY_CHANNEL_SECRET} # 你的 LINE Pay Channel Secret Key
  api-url: ${LINEPAY_API_URL} # Sandbox 環境的 API URL
confirm-url: ${LINEPAY_CONFIRM_URL} # LINE Pay 付款成功後導回的後端確認網址